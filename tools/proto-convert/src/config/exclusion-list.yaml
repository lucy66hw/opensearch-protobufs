# Schemas to exclude from proto generation
# These schemas and their nested dependencies will not be included
excluded_schemas:
   - Aggregations
   - AggregationContainer
  # Specific query types to exclude
   - ScriptScoreQuery
   - PercolateQuery
   - Suggester
   - NeuralQuery
